public void addOrdered () {
    int numMin = Entero. MIN_VALUE;
        currentNode;
        actual = cabeza;
        nodo currentAux;
        actualAux = cabeza;
    
}


@Anular
borrado booleano público (T d) {
    boolean viewDeleted = false;
    DoubleNode <T> currentNode = head;
    DoubleNode <T> antes = nulo;
    
    while (currentNode! = null) {
        if (currentNode.getData () == d) {
            si (antes == nulo) {
                head = head.getNextNode ();
                currentNode.srtNextNode (nulo);
                currentNode = head;
            }
            demás{
                before.setNextNode (currentNode.getNextNode ());
                currentNode.setNextNode (nulo);
                currentNode = before.getNextNode ();
            }
        }demás{
            before = currentNode;
            currentNode = currentNode.getNextNode ();
        }
    }

    return checkDeleted
}
